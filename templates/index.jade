extends layout

block content
  each file in paginate.files
    article.content
      header.header
        small.header-metadata
          time.timestamp(datetime=file.date)= moment(file.date).format('MMMM D, YYYY')
        h2.content-title
          a(href='/' + file.path)= file.title
      section.content-article!= file.snippet
  nav.navigation.cf
    if paginate.previous
      a.btn.pull-right(href='/' + paginate.previous.path)
        | Newer&nbsp;&nbsp;
        i.icon-right-dir
    if paginate.next
      a.btn.pull-left(href='/' + paginate.next.path)
        i.icon-left-dir
        | &nbsp;&nbsp;Older
